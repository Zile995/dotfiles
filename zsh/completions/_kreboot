#compdef kreboot

_kreboot() {
  if (( CURRENT == 2 )); then
    compadd $('find' /usr/lib/modules/* \
      -mindepth 1 -maxdepth 1 \
      ! -path '*/\.*' ! -path '*/build' -type d \
      -exec 'pacman' -Qqo {} \; | \
      'sort' -n)
  fi
}
